#!/usr/bin/env bash

# Requires this option:
# security.pam.services.sudo.u2fAuth = true;

yubico_dir="$HOME/.config/Yubico"
mkdir -p "$yubico_dir"
nix shell nixpkgs#pam_u2f -c pamu2fcfg >> "$yubico_dir/u2f_keys"
